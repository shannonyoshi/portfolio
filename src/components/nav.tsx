import React from "react";

const Navigation = () => {
return (
  <nav>
    <a href="#">Portfolio</a>
    <a href="#">Experience</a>
    <a href="#">Resume</a>
    <a href="#">About Me</a>
  </nav>
)
}

export default Navigation;